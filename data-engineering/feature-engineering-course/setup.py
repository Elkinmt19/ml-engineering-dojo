# Build-in imports
from setuptools import setup, find_packages

setup(
    name="feature_engineering_course",
    version="0.0.1",
    description="""
        This is a project that contains notebooks and exercises of feature engineering.
    """,
    packages=find_packages()
)